//An array is a numerically indexed map of values. An array doesn't store one value, it stores a list of values//
let rainbowColors = ['Red', 'Orange', 'Yellow', 'Green', 'Blue', 'Indigo', 'Violet'];
let raceWinners = [33, 72, 64];
let myFavoriteThings =['Broccoli', 'Whipped Cream', 'cats'];
let pets =['kipper', 'Darth Vader', 'Wiley', 'Thyme'];
let favoriteNums=[12, 24, 45, 75];
let planets =['Mars', 'venus', 'Saturn', 'Earth'];
let stuff =['phone', 'fan', 'lamp', 'computer'];

console.log(myFavoriteThings[2]);
console.log("My Favorite Things", myFavoriteThings[0]);
console.log(pets[0]);
console.log(favoriteNums[1]);
console.log(planets[2]);
console.log(stuff[3]);

//*Create an array like a variable with any name.
//*Use square brackets [] to collect/initialize values.
//*We separate items with commas within the brackets.
//*We can have a collection of one type  OR
//*We can assign multiple types:

var nySecereteSpyInfo = ["James Bond", 007, true]; // a string, a number, and a boolean//

//index starts at // [0],     [1],     [2]
var countryArray = ["USA", "Russia", "China"]